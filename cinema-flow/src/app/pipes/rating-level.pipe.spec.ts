import { RatingLevelPipe } from './rating-level.pipe';

describe('RatingLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
